kind = "web"
previewPath = "/"
title = "Road Trip Planner"
version = "1.0.0"
id = "artifacts/road-trip-planner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20727

[services.development]
run = "pnpm --filter @workspace/road-trip-planner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/road-trip-planner", "run", "build" ]
publicDir = "artifacts/road-trip-planner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20727"
BASE_PATH = "/"
